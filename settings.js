// １度に移動する距離(px)
const HOHABA_PX = 20;

// アバター画像の横幅(px)
const AVATAR_WIDTH = 50;

// コンテンツの大きさ(px)
const CONTAINER_HEIGHT = 500;
const CONTAINER_WIDTH = 700;

const MIN_LEFT_CONTAINER = 20;
const MAX_LEFT_CONTAINER = CONTAINER_WIDTH - AVATAR_WIDTH;
const MIN_TOP_CONTAINER = 30;
const MAX_TOP_CONTAINER = CONTAINER_HEIGHT - AVATAR_WIDTH;

// 廊下の縦幅(px)
const MIN_TOP_ROUKA = 160;
const MAX_TOP_ROUKA = 360;

// ページ送り
const PrevPageKey = 'p'; //前のページを表示
const NextPageKey = 'n'; //次のページを表示

// 背景画像
// 部屋番号(0始まり): 画像名
const BACKGROUND_IMAGES = {
  3: 'sample.png'
};
